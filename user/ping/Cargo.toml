[package]
name = "ping"
version = "0.1.0"
edition = "2021"

[dependencies]
nova-net = { path = "../nova-net" }

[profile.release]
panic = "abort"
opt-level = "s"
strip = true
lto = true
codegen-units = 1
